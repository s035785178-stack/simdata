workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 30

    environment:
      vars:
        PACKAGE_NAME: "com.stereotip.simdata"

    scripts:
      - name: Build APK
        script: |
          gradle assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
